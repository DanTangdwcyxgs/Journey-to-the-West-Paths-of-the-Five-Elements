# 《西游：五行之路》项目持续记忆

> 用途：供后续工作模型接手。每次工作会话结束后，在本文件追加一条记录。不要删除既有记录；以最新记录为当前状态，并保留历史决策与已验证结果。

## 2026-09-05 · 视觉重构阶段 / 会话记录

### 用户核心反馈
- 当前像素人物此前存在严重问题：基本是“一个方框一个圆形”，不像真正可玩的 JRPG 像素角色。
- 用户要求继续推进，但采用“一屏一验收”：完成一个画面后先让用户进入查看，再做下一屏。
- 本阶段视觉目标：真正的低分辨率像素 JRPG，而不是程序几何占位。角色必须通过轮廓、发型/头饰、服装、武器、阴影、配色和动作空间形成识别度。

### 本阶段已完成
- 建立独立像素角色资产：悟空、唐僧、八戒、悟净、龙马、黄风怪。
- 建立像素场景资产：旅途场景、黄风战斗场景、取经路线地图。
- `ui/visual_overlay.gd` 已从程序几何人物切换到加载 SVG 像素资产；统一使用 `TEXTURE_FILTER_NEAREST`，保持像素边缘。
- `ui/character_portrait.gd` 已切换到同套像素角色资源，避免场景与肖像画风分裂。
- Runtime 回归已增加像素资产加载验证。

### 工程验证
- Godot Runtime #238：SUCCESS。
- 最新已验证 Runtime commit：`1ef2c0cf02973aafbf002795ee37c4e8a7809cf1`。
- Runtime suite 当前通过 28 项测试。
- Web Demo 在同一视觉提交线上重新触发构建；期间曾暴露 `character_portrait.gd` 类型推断错误，已修正并重新提交。

### 发现的 CI 问题与处理
- Web 构建早期导入阶段曾出现 `CharacterPortrait` 的 `target` 类型无法推断，且 Warning treated as error。
- 同期还出现字体资源尚未完成导入的问题；工作流已经有 Godot asset import warm-up。
- 当前应该以最新 Web Demo run 的最终状态为准，不把“pending / in_progress”描述成已经部署成功。

### 当前视觉代码现状
- `ui/visual_overlay.gd`：已使用像素 SVG 场景与角色纹理；主菜单、旅途、战斗、黄风场景均能映射到对应视觉资源。
- 当前旅途和主菜单仍保留较多传统动态 UI，需要进一步按“一屏一验收”重构视觉布局。
- `ui/battle_ui.gd` 已开始向“战场为视觉中心”的 JRPG 布局调整，但尚未作为本轮用户验收屏幕。
- 当前主菜单 `ui/main_menu.gd` 仍是旧式三栏数据/文字布局，虽然 VisualOverlay 能提供背景与角色，但主菜单整体还不够像成品 JRPG。

### 一屏一验收工作规则
1. 一次只修改一个可直接进入查看的画面。
2. 修改完成后立刻运行/等待 Runtime 与 Web CI 验证。
3. 明确告诉用户“这一屏已经完成，可以进入看”，然后暂停该屏，不同时推进下一屏。
4. 用户反馈后才进入下一屏。
5. 优先顺序建议：主菜单 → 个人序章第一场景 → 共享旅途场景 → 黄风岭 → 战斗 → 地图/其他辅助界面。

### 下一步
- 当前应优先完成 **主菜单单屏**。
- 主菜单目标：不再使用大段文字+三栏表格作为视觉主体；应以完整西游主题背景、五人独立像素角色、标题/开始入口、角色选择状态和少量必要信息构成真正的游戏标题画面。
- 完成后只让用户验收这一屏，不要继续自动推进第二屏。

### 后续模型接手注意
- 不要重新采用“圆头 + 方身体 + 简单线条”的程序人物。
- 不要把“有 SVG 资源”直接等同于“美术完成”；仍需检查画面构图、缩放、遮挡层级、角色比例、UI 占比和可玩感。
- 用户希望程序持续直接修改 GitHub 仓库、测试并提交，不要反复询问已经明确的目标。
- 每个工作会话都必须把实际改动、测试、当前 commit、未完成事项和下一步写入本文件。
